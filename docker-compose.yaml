services:
  app:
    container_name: ontv-front-prod
    image: ontv/frontend-prod
    hostname: ontv-front
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - 3000:3000