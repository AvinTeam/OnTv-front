// import { Tab } from "@/types/tab.type"
export type NewestCardProps = {
  data: {
    alt: string;
    description: string;
    title: string;
    seen: number;
    id: number;
    path: string;
    poster: any[];
  };
};
